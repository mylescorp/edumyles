# EduMyles Audit Checklist - Current State

Generated from the 2026-04-04 audit after verifying the current codebase.

| Area                      | Fixed Since Audit                                                                                                                                                                                                                                                                                                                                                                        | Still Open                                                                                                                                                                                                                                                   |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Critical blockers         | All 10 reported merge-conflict files are now clean.<br>`/admin/*` now has `RoleGuard`.<br>`convex/tickets.ts` now scopes `getTickets` and `getSLAStats` by tenant.<br>`examinations` now exists in `convex/schema.ts`.<br>Convex payment callback routes now exist for M-Pesa, Airtel, and Stripe in `convex/http.ts`.<br>Parent `initiatePayment` is now wired to real payment actions. | No unresolved critical blockers from the original audit remain in the areas re-checked here.                                                                                                                                                                 |
| Admin routes              | `/admin/hr/performance` exists.<br>`/admin/academics/exams` exists.<br>`/admin/academics/exams/create` exists.<br>`/admin/academics/assignments/create` exists.<br>`/admin/academics/reports` exists.<br>`/admin/academics/classes/create` exists.<br>HR dashboard no longer hardcodes `Open Positions = 3`; it now shows live pending leave requests.<br>Roles settings now load from backend role definitions.<br>Reports page now shows live summary values instead of placeholder dashes. | Reports is still primarily a link hub rather than a dedicated analytics surface.                                                                                                                                                                              |
| Guards and access control | `ModuleAccessGuard` now exists in alumni and portal admin layouts.<br>Admin layout now has both `RoleGuard` and `ModuleAccessGuard`.<br>`frontend/src/middleware.ts` now exists and re-exports the existing proxy-based server-side guard.<br>`/support/*` is now protected in the server-side guard.<br>Authenticated users are redirected away from login/signup pages server-side. | Route protection is still powered by the existing `proxy.ts` architecture rather than a larger App Router auth redesign.                                                                                                                                     |
| Payments                  | Gateway initiation exists for M-Pesa, Airtel, Stripe, and bank transfer.<br>Bank transfer manual verification flow exists.<br>Convex HTTP callback/webhook endpoints now exist for M-Pesa, Airtel, and Stripe.<br>Parent backend payment initiation mutation now calls real M-Pesa, Airtel, Stripe, and bank-transfer actions.                                                           | Live provider callback URLs and webhook secrets still need environment-level validation in deployed environments.                                                                                                                                            |
| Communications            | Single-send SMS and email remain implemented.<br>In-app notifications and push notification batching remain implemented.<br>`sendBulkSMS` now resolves campaign recipients, sends batched messages, persists `messageRecords`, and updates campaign stats.<br>`sendBulkEmail` now resolves campaign recipients, sends batched emails, persists `messageRecords`, and updates campaign stats. | Delivery/open/click webhooks for comms providers are still not wired back into `messageRecords` after initial send.                                                                                                                                          |
| Shared/business logic     | `GradingSystem.calculateWeightedAverage` is fixed to use `totalWeightedScore / totalWeight`.                                                                                                                                                                                                                                                                                             | Many backend status fields still use open `v.string()` validators instead of tighter unions.<br>No shared Convex response types yet.                                                                                                                         |
| Frontend data loading     | Core pages still use real Convex queries.<br>Sidebar admissions and finance badges now use live pending counts.                                                                                                                                                                                                                                                                          | `DataTable` still paginates client-side only.<br>No broad cursor-based server pagination rollout yet.                                                                                                                                                        |
| Student routing           | Student portal routes remain implemented.<br>Legacy `/student/*` requests now redirect server-side to canonical `/portal/student/*` routes.                                                                                                                                                                                                                                               | The legacy `frontend/src/app/(portal)/student/*` files still exist on disk and could still be removed later for cleanup.                                                                                                                                     |
| Scheduled jobs            | Cron coverage now includes scheduled reports, expired session cleanup, overdue invoice alerts, SLA breach detection, and stale payment reconciliation.                                                                                                                                                                                                                                     | Overdue invoice alerts currently create in-app notifications only; they do not yet fan out to SMS/email automatically.                                                                                                                                      |
| Mobile                    | Mobile student, parent, and teacher flows remain implemented.                                                                                                                                                                                                                                                                                                                            | No `app.json` or `app.config.ts` under `mobile/`.<br>`mobile/package.json` still uses `expo-router/entry` while `mobile/src/App.tsx` still drives manual tabs.<br>No deep-link configuration found.<br>Mobile theme still diverges from shared theme tokens. |
| CI/CD and infra           | CI, preview deploy, and production deploy workflows exist.                                                                                                                                                                                                                                                                                                                               | No mobile build/test job in CI.<br>No Convex schema validation in CI.<br>E2E still runs only the smoke suite in CI.<br>No staging environment is defined.                                                                                                    |
| Deferred/disabled modules | None.                                                                                                                                                                                                                                                                                                                                                                                    | `convex/actions/ai/index.ts` is still disabled.<br>`convex/modules/pm/index.ts` still excludes GitHub and deploy exports due to type issues.                                                                                                                 |

## Recommended Order From Here

1. Remove the legacy `frontend/src/app/(portal)/student/*` files once you are comfortable dropping the old path set completely.
2. Add delivery-status reconciliation for bulk email/SMS beyond initial send success/failure.
3. Roll out server-side pagination for large admin tables.
4. Clean up mobile config, entry-point alignment, and theme divergence.
5. Re-enable the AI support and PM GitHub/deploy exports once their type/runtime issues are resolved.
